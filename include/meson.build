# Install the header in the default header library in the conduit folder
install_hdr_path = get_option('includedir')

install_subdir(
    'conduit',
    install_dir : install_hdr_path,
    strip_directory : false
)
